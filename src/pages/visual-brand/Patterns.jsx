import BlockField from "./BlockField.jsx";

// Patterns (Figma 51:2932) — the building blocks brought together with the
// visible grid and a density slider. Static size (no auto-cycle).
export default function Patterns() {
  return <BlockField grid densitySlider />;
}
